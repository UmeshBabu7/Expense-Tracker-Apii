[project]
name = "expense-tracker"
version = "0.1.0"
description = "Expense Tracker API — Django REST backend for personal spending."
requires-python = ">=3.11"
dependencies = [
    "dj-database-url>=3.1.2",
    "django==5.0.6",
    "djangorestframework==3.15.1",
    "python-decouple>=3.8",
    "python-dotenv==1.0.1",
    "requests>=2.34.2",
]
